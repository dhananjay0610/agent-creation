{
  "name": "playwright-ts-starter",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:signup": "playwright test --grep 'Sign up CTA'",
    "test:search-input": "playwright test --grep 'Search input visibility'",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0",
    "@types/node": "^22.9.1",
    "typescript": "^5.6.3"
  }
}
